["er.eventlog.import-anchor/1",{"authority":{"logical_scope":"eventlog","stream_identity":"01a0d330-f098-7710-9e63-51c8a8bda507","tenant":"planning"},"completeness":"available_evidence_only","evidence":[{"event":{"args":{"command":"create-entity","data":{"body":"## Outcome\n\nWriting many blobs inside one atomic append group costs one durability barrier for the group, not\none `fsync` per blob. The provider's durability guarantee is unchanged: nothing in the group is\nobservable before the barrier, and a crash before it leaves none of the group behind.\n\n## Why\n\nMeasured 2026-09-21 by unit 9 of wave-validate-v2-20260920 (AEP), on a copy of the real ESS\nplanning store through the recorded Eventlog store (ER 8569da2, provider 76aad5e):\n\n| quantity | value |\n| --- | --- |\n| imports in one ESS migration `apply` | 7,810 (3,222 evidence blobs among them) |\n| `fsync` calls in a 5.16 s instrumented run | 1,255, **3.16 s of wall time (61 %)** under `strace -c -w` |\n| the same under `strace -c` | 18 ms — CPU time only; `fsync` burns none, which misled a first reading |\n| ESS `apply` after one capture per batch (ER story) | ≈ 6.5 min, most of it durability |\n| plus one barrier per group (this story) | ≈ 60 s projected |\n\n`put_blob` (`crates/eventlog-file`) syncs each blob as it lands. Inside an append group (ADR 05,\n`docs/design/file-provider.md`) the group is what becomes durable, so per-blob barriers buy nothing\nthe group's own barrier does not.\n\n## Acceptance\n\n- Red first: a counting case (the crate's test hooks) that writes N blobs in one group and asserts\n  one barrier for the group, not N; fails at 76aad5e.\n- The crash-consistency cases for groups stay green: a group interrupted before its barrier leaves\n  no blob or frame behind; after the barrier, all of it is present; the existing conformance suite\n  under `crates/eventlog-conformance/` is not edited.\n- Byte identity: blob keys and contents are unchanged; `verify` on a store written both ways is\n  byte-identical.\n- Decisions after pass 2 (2026-09-21, coordinator): the port method\n  `AtomicEventStore::append_group_guarded_with_blobs` **fails closed** by default — an inheriting\n  provider refuses and publishes nothing; only a provider that implements admission-before-bind\n  overrides it (the file provider does). Dedup identity is decided against the digest set the group\n  **committed**, never against live blob presence; a committed digest whose blob was later deleted\n  still deduplicates. `verify_bound` runs after admission, inside the lock. The `object_syncs`\n  charge lives in the function that performs the sync. The module's limit paragraph names every\n  claim no case exercises (dropped fsync, mid-batch failure, disposal masking).\n- Exception to \"the conformance suite is not edited\": `eventlog-conformance` gains cases for the\n  new port method (refusal binds nothing; dedup by committed digests; a non-implementing provider\n  refuses with the fail-closed code), run against all three providers. Nothing existing in that\n  crate is changed.\n- `docs/design/file-provider.md` states where the barrier is for grouped writes.\n- `scripts/gate.sh` under the CI fixture conditions green (evidence/eventlog-tls-gate.sh);\n  then Eventlog → ER → AEP re-pin.\n- Related, measured afterwards on the ESS copy: if `apply` still misses 10 s, the remaining share is\n  named (story:file-capture-blob-digests-without-rehashing is the expected candidate) with the number.\n","status":"draft","title":"Blobs written inside one append group take one durability barrier, not one fsync each"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/grouped-blob-writes-take-one-durability-barrier"},"changed":{"$aep":{"archived":false,"metadata":{"created_at":1790199280402,"id":"ent-ae6311829807d2dea7a375a934683ad3","locator":"ep://planning/store/story/grouped-blob-writes-take-one-durability-barrier","provenance":{"created_by":"human:timo","updated_by":"human:timo"},"revision":1,"type":"aep.story/v1","updated_at":1790199280402}},"body":"## Outcome\n\nWriting many blobs inside one atomic append group costs one durability barrier for the group, not\none `fsync` per blob. The provider's durability guarantee is unchanged: nothing in the group is\nobservable before the barrier, and a crash before it leaves none of the group behind.\n\n## Why\n\nMeasured 2026-09-21 by unit 9 of wave-validate-v2-20260920 (AEP), on a copy of the real ESS\nplanning store through the recorded Eventlog store (ER 8569da2, provider 76aad5e):\n\n| quantity | value |\n| --- | --- |\n| imports in one ESS migration `apply` | 7,810 (3,222 evidence blobs among them) |\n| `fsync` calls in a 5.16 s instrumented run | 1,255, **3.16 s of wall time (61 %)** under `strace -c -w` |\n| the same under `strace -c` | 18 ms — CPU time only; `fsync` burns none, which misled a first reading |\n| ESS `apply` after one capture per batch (ER story) | ≈ 6.5 min, most of it durability |\n| plus one barrier per group (this story) | ≈ 60 s projected |\n\n`put_blob` (`crates/eventlog-file`) syncs each blob as it lands. Inside an append group (ADR 05,\n`docs/design/file-provider.md`) the group is what becomes durable, so per-blob barriers buy nothing\nthe group's own barrier does not.\n\n## Acceptance\n\n- Red first: a counting case (the crate's test hooks) that writes N blobs in one group and asserts\n  one barrier for the group, not N; fails at 76aad5e.\n- The crash-consistency cases for groups stay green: a group interrupted before its barrier leaves\n  no blob or frame behind; after the barrier, all of it is present; the existing conformance suite\n  under `crates/eventlog-conformance/` is not edited.\n- Byte identity: blob keys and contents are unchanged; `verify` on a store written both ways is\n  byte-identical.\n- Decisions after pass 2 (2026-09-21, coordinator): the port method\n  `AtomicEventStore::append_group_guarded_with_blobs` **fails closed** by default — an inheriting\n  provider refuses and publishes nothing; only a provider that implements admission-before-bind\n  overrides it (the file provider does). Dedup identity is decided against the digest set the group\n  **committed**, never against live blob presence; a committed digest whose blob was later deleted\n  still deduplicates. `verify_bound` runs after admission, inside the lock. The `object_syncs`\n  charge lives in the function that performs the sync. The module's limit paragraph names every\n  claim no case exercises (dropped fsync, mid-batch failure, disposal masking).\n- Exception to \"the conformance suite is not edited\": `eventlog-conformance` gains cases for the\n  new port method (refusal binds nothing; dedup by committed digests; a non-implementing provider\n  refuses with the fail-closed code), run against all three providers. Nothing existing in that\n  crate is changed.\n- `docs/design/file-provider.md` states where the barrier is for grouped writes.\n- `scripts/gate.sh` under the CI fixture conditions green (evidence/eventlog-tls-gate.sh);\n  then Eventlog → ER → AEP re-pin.\n- Related, measured afterwards on the ESS copy: if `apply` still misses 10 s, the remaining share is\n  named (story:file-capture-blob-digests-without-rehashing is the expected candidate) with the number.\n","status":"draft","title":"Blobs written inside one append group take one durability barrier, not one fsync each"},"entity":"story","from_state":null,"id":"ent-ae6311829807d2dea7a375a934683ad3","payload":{"actor":"human:timo","at":1790199280402,"causation":"cmd-child-6f7a00fec80eb837ee6caa9047f26f8f0e0f82d82ac94e4202ca2c7d7b337384","correlation":"protocol-artifact-new","event_id":"aep.entity:ent-ae6311829807d2dea7a375a934683ad3@1#0~524b2f8061a04f94","executor":null,"recorded_at":"2026-09-23T21:34:40Z"},"revision":1,"to_state":"draft","type":"aep.entity.create/v1","version":1},"kind":"event"},{"event":{"args":{"command":"create-relation","kind":"serves","source":"ent-ae6311829807d2dea7a375a934683ad3","target":"ent-4010225477ad888c71c1e8c146ef8689"},"changed":{},"entity":"story","from_state":"draft","id":"ent-ae6311829807d2dea7a375a934683ad3","payload":{"actor":"human:timo","at":1790199319441,"causation":"cmd-child-347fa60d86580ff8de9660b03ac1503a4d393b796db4796376a23725b908ede4","correlation":"protocol-artifact-relate","event_id":"aep.entity:ent-ae6311829807d2dea7a375a934683ad3@1#0~73f2dbbba53587c4","executor":null,"recorded_at":"2026-09-23T21:35:19Z"},"revision":1,"to_state":"draft","type":"aep.relation.create/v1","version":1},"kind":"event"},{"event":{"args":{"command":"record-evidence","kind":"test_result","reference":"git:beyond10x/eventlog@7fbd37cf","source":"evidence/eventlog-tls-gate.sh on integrate/ess-evolution-eventlog-20260915 at 7fbd37cf: exit 0, 337 passed, 0 failed, 53 lanes","target":"ent-ae6311829807d2dea7a375a934683ad3"},"changed":{},"entity":"story","from_state":"draft","id":"ent-ae6311829807d2dea7a375a934683ad3","payload":{"actor":"human:timo","at":1790028300000,"causation":"cmd-child-c1969b7b872119a602f9948083657f4b9854b208c1e565b9d3850ffd6b604781","correlation":"protocol-artifact-evidence","event_id":"aep.entity:ent-ae6311829807d2dea7a375a934683ad3@1#0~e814956177e9feec","executor":null,"recorded_at":"2026-09-21T22:05:00Z"},"revision":1,"to_state":"draft","type":"aep.evidence.record/v1","version":1},"kind":"event"},{"event":{"args":{"command":"move-status","decided_on":"{\"recorded\":{\"test_result\":1}}","target":"ent-ae6311829807d2dea7a375a934683ad3","to":"proposed"},"changed":{"$aep":{"archived":false,"metadata":{"created_at":1790199280402,"id":"ent-ae6311829807d2dea7a375a934683ad3","locator":"ep://planning/store/story/grouped-blob-writes-take-one-durability-barrier","provenance":{"created_by":"human:timo","updated_by":"human:timo"},"revision":2,"type":"aep.story/v1","updated_at":1790199410091}},"status":"proposed"},"entity":"story","from_state":"draft","id":"ent-ae6311829807d2dea7a375a934683ad3","payload":{"actor":"human:timo","at":1790199410091,"causation":"cmd-child-1b79b1eeaecb809e50c4ae24659897f266d14ce234c5c7529cb07038652976c2","correlation":"protocol-artifact-move","event_id":"aep.entity:ent-ae6311829807d2dea7a375a934683ad3@2#0~099d3a54299a5f38","executor":null,"recorded_at":"2026-09-23T21:36:50Z"},"revision":2,"to_state":"proposed","type":"aep.status.move/v1","version":1},"kind":"event"},{"event":{"args":{"command":"move-status","decided_on":"{\"recorded\":{\"test_result\":1}}","target":"ent-ae6311829807d2dea7a375a934683ad3","to":"active"},"changed":{"$aep":{"archived":false,"metadata":{"created_at":1790199280402,"id":"ent-ae6311829807d2dea7a375a934683ad3","locator":"ep://planning/store/story/grouped-blob-writes-take-one-durability-barrier","provenance":{"created_by":"human:timo","updated_by":"human:timo"},"revision":3,"type":"aep.story/v1","updated_at":1790199464444}},"status":"active"},"entity":"story","from_state":"proposed","id":"ent-ae6311829807d2dea7a375a934683ad3","payload":{"actor":"human:timo","at":1790199464444,"causation":"cmd-child-e917ce83f0534022970fb8489a1433268eb404f97f010c476b2c19efda7e8bf4","correlation":"protocol-artifact-move","event_id":"aep.entity:ent-ae6311829807d2dea7a375a934683ad3@3#0~9e921d3950b544d4","executor":null,"recorded_at":"2026-09-23T21:37:44Z"},"revision":3,"to_state":"active","type":"aep.status.move/v1","version":1},"kind":"event"},{"event":{"args":{"command":"move-status","decided_on":"{\"recorded\":{\"test_result\":1}}","target":"ent-ae6311829807d2dea7a375a934683ad3","to":"implemented"},"changed":{"$aep":{"archived":false,"metadata":{"created_at":1790199280402,"id":"ent-ae6311829807d2dea7a375a934683ad3","locator":"ep://planning/store/story/grouped-blob-writes-take-one-durability-barrier","provenance":{"created_by":"human:timo","updated_by":"human:timo"},"revision":4,"type":"aep.story/v1","updated_at":1790199507364}},"status":"implemented"},"entity":"story","from_state":"active","id":"ent-ae6311829807d2dea7a375a934683ad3","payload":{"actor":"human:timo","at":1790199507364,"causation":"cmd-child-e69366259580a65ce2cb9b5eb65c3c90e50ae0766dc64f234c9d5bd1383ce924","correlation":"protocol-artifact-move","event_id":"aep.entity:ent-ae6311829807d2dea7a375a934683ad3@4#0~8b5dbd24c7dd32b4","executor":null,"recorded_at":"2026-09-23T21:38:27Z"},"revision":4,"to_state":"implemented","type":"aep.status.move/v1","version":1},"kind":"event"}],"instance":{"entity":"story","fields":{"body":"## Outcome\n\nWriting many blobs inside one atomic append group costs one durability barrier for the group, not\none `fsync` per blob. The provider's durability guarantee is unchanged: nothing in the group is\nobservable before the barrier, and a crash before it leaves none of the group behind.\n\n## Why\n\nMeasured 2026-09-21 by unit 9 of wave-validate-v2-20260920 (AEP), on a copy of the real ESS\nplanning store through the recorded Eventlog store (ER 8569da2, provider 76aad5e):\n\n| quantity | value |\n| --- | --- |\n| imports in one ESS migration `apply` | 7,810 (3,222 evidence blobs among them) |\n| `fsync` calls in a 5.16 s instrumented run | 1,255, **3.16 s of wall time (61 %)** under `strace -c -w` |\n| the same under `strace -c` | 18 ms — CPU time only; `fsync` burns none, which misled a first reading |\n| ESS `apply` after one capture per batch (ER story) | ≈ 6.5 min, most of it durability |\n| plus one barrier per group (this story) | ≈ 60 s projected |\n\n`put_blob` (`crates/eventlog-file`) syncs each blob as it lands. Inside an append group (ADR 05,\n`docs/design/file-provider.md`) the group is what becomes durable, so per-blob barriers buy nothing\nthe group's own barrier does not.\n\n## Acceptance\n\n- Red first: a counting case (the crate's test hooks) that writes N blobs in one group and asserts\n  one barrier for the group, not N; fails at 76aad5e.\n- The crash-consistency cases for groups stay green: a group interrupted before its barrier leaves\n  no blob or frame behind; after the barrier, all of it is present; the existing conformance suite\n  under `crates/eventlog-conformance/` is not edited.\n- Byte identity: blob keys and contents are unchanged; `verify` on a store written both ways is\n  byte-identical.\n- Decisions after pass 2 (2026-09-21, coordinator): the port method\n  `AtomicEventStore::append_group_guarded_with_blobs` **fails closed** by default — an inheriting\n  provider refuses and publishes nothing; only a provider that implements admission-before-bind\n  overrides it (the file provider does). Dedup identity is decided against the digest set the group\n  **committed**, never against live blob presence; a committed digest whose blob was later deleted\n  still deduplicates. `verify_bound` runs after admission, inside the lock. The `object_syncs`\n  charge lives in the function that performs the sync. The module's limit paragraph names every\n  claim no case exercises (dropped fsync, mid-batch failure, disposal masking).\n- Exception to \"the conformance suite is not edited\": `eventlog-conformance` gains cases for the\n  new port method (refusal binds nothing; dedup by committed digests; a non-implementing provider\n  refuses with the fail-closed code), run against all three providers. Nothing existing in that\n  crate is changed.\n- `docs/design/file-provider.md` states where the barrier is for grouped writes.\n- `scripts/gate.sh` under the CI fixture conditions green (evidence/eventlog-tls-gate.sh);\n  then Eventlog → ER → AEP re-pin.\n- Related, measured afterwards on the ESS copy: if `apply` still misses 10 s, the remaining share is\n  named (story:file-capture-blob-digests-without-rehashing is the expected candidate) with the number.\n","document":{"events":[],"fields":{"$aep":{"archived":false,"metadata":{"created_at":1790199280402,"id":"ent-ae6311829807d2dea7a375a934683ad3","locator":"ep://planning/store/story/grouped-blob-writes-take-one-durability-barrier","provenance":{"created_by":"human:timo","updated_by":"human:timo"},"revision":4,"type":"aep.story/v1","updated_at":1790199507364}},"body":"## Outcome\n\nWriting many blobs inside one atomic append group costs one durability barrier for the group, not\none `fsync` per blob. The provider's durability guarantee is unchanged: nothing in the group is\nobservable before the barrier, and a crash before it leaves none of the group behind.\n\n## Why\n\nMeasured 2026-09-21 by unit 9 of wave-validate-v2-20260920 (AEP), on a copy of the real ESS\nplanning store through the recorded Eventlog store (ER 8569da2, provider 76aad5e):\n\n| quantity | value |\n| --- | --- |\n| imports in one ESS migration `apply` | 7,810 (3,222 evidence blobs among them) |\n| `fsync` calls in a 5.16 s instrumented run | 1,255, **3.16 s of wall time (61 %)** under `strace -c -w` |\n| the same under `strace -c` | 18 ms — CPU time only; `fsync` burns none, which misled a first reading |\n| ESS `apply` after one capture per batch (ER story) | ≈ 6.5 min, most of it durability |\n| plus one barrier per group (this story) | ≈ 60 s projected |\n\n`put_blob` (`crates/eventlog-file`) syncs each blob as it lands. Inside an append group (ADR 05,\n`docs/design/file-provider.md`) the group is what becomes durable, so per-blob barriers buy nothing\nthe group's own barrier does not.\n\n## Acceptance\n\n- Red first: a counting case (the crate's test hooks) that writes N blobs in one group and asserts\n  one barrier for the group, not N; fails at 76aad5e.\n- The crash-consistency cases for groups stay green: a group interrupted before its barrier leaves\n  no blob or frame behind; after the barrier, all of it is present; the existing conformance suite\n  under `crates/eventlog-conformance/` is not edited.\n- Byte identity: blob keys and contents are unchanged; `verify` on a store written both ways is\n  byte-identical.\n- Decisions after pass 2 (2026-09-21, coordinator): the port method\n  `AtomicEventStore::append_group_guarded_with_blobs` **fails closed** by default — an inheriting\n  provider refuses and publishes nothing; only a provider that implements admission-before-bind\n  overrides it (the file provider does). Dedup identity is decided against the digest set the group\n  **committed**, never against live blob presence; a committed digest whose blob was later deleted\n  still deduplicates. `verify_bound` runs after admission, inside the lock. The `object_syncs`\n  charge lives in the function that performs the sync. The module's limit paragraph names every\n  claim no case exercises (dropped fsync, mid-batch failure, disposal masking).\n- Exception to \"the conformance suite is not edited\": `eventlog-conformance` gains cases for the\n  new port method (refusal binds nothing; dedup by committed digests; a non-implementing provider\n  refuses with the fail-closed code), run against all three providers. Nothing existing in that\n  crate is changed.\n- `docs/design/file-provider.md` states where the barrier is for grouped writes.\n- `scripts/gate.sh` under the CI fixture conditions green (evidence/eventlog-tls-gate.sh);\n  then Eventlog → ER → AEP re-pin.\n- Related, measured afterwards on the ESS copy: if `apply` still misses 10 s, the remaining share is\n  named (story:file-capture-blob-digests-without-rehashing is the expected candidate) with the number.\n","status":"implemented","title":"Blobs written inside one append group take one durability barrier, not one fsync each"},"lifecycle_state":"implemented"},"title":"Blobs written inside one append group take one durability barrier, not one fsync each"},"id":"ent-ae6311829807d2dea7a375a934683ad3","lifecycle_state":"implemented","revision":4,"version":1},"order":"per_kind_only","subject":["story","ent-ae6311829807d2dea7a375a934683ad3"]}]